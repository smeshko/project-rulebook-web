# Phase Configuration: ship
# Generated by ADW Init Wizard
#
# Uncomment settings to customize this phase.

enabled: true
# timeout_seconds: 900  # Phase-specific timeout

# pre_hook: null  # Shell command before phase
# post_hook: null  # Shell command after phase

input_files:
  docs: /docs/README.md
  getting-started: /docs/development/getting-started.md

# === Ship Phase Settings ===
# commands:
#   version_bump: null  # Version bump command
#   build: null  # Build command for deployment
#   publish: null  # Publish/deploy command

# post_publish:  # Commands after publishing
#   - git push --tags

# pr:
#   merge_on_success: false  # Auto-merge PR
#   delete_branch_on_merge: true  # Delete branch
#   merge_method: squash  # merge/squash/rebase
