# ADW Project Configuration
# Generated by ADW Init Wizard on 2026-03-22
#
# Settings shown as comments are optional with default values.
# Uncomment and modify to customize behavior.

# === Core Settings ===
name: javascript
language: javascript
platform: web
# framework: null  # Framework being used (e.g., fastapi)

# === Commands ===
test_command: npm test
# build_command: null  # Command to build the project

# === Git Integration ===
git:
  branch_prefix: feature/
  # skip_hooks: false  # Skip pre-commit hooks
  base_branch: staging

# === Task Manager ===
task_manager:
  type: linear
  team_key: RULE
  sync_comments: true
  auto_close: true
  # state_mapping:
  #   plan: In Progress
  #   build: In Progress
  #   validate: In Review
  #   document: In Review
  #   ship: Done
  #   failed: In Progress
  # labels:
  #   enabled: true  # Enable label management
  #   prefix: "adw:"  # Prefix for ADW-managed labels

# === Worktree & Ports ===
# worktree:
#   enabled: true  # Use worktree isolation
#   base_dir: "trees"  # Directory for worktrees
#   max_concurrent: 15  # Maximum concurrent runs
#   port_range:
#     backend_start: 9100  # Backend port base
#     frontend_start: 9200  # Frontend port base

# === LLM Configuration ===
# llm:
#   path: "claude"  # Path to Claude Code executable

# === Security ===
# security:
#   blocked_patterns: []  # Additional patterns to block
#   blocked_env_files: []  # Additional file patterns to block

# === Webhook Server ===
# webhook:
#   enabled: false  # Enable webhook server
#   port: 8000
#   host: "0.0.0.0"
